%div.row
  %div.col-lg-6
    %div.well
      %form{action: "/people", method: "post", class: "form-inline"}
        %div.form-group
          %label Name:
          %input{type: "text", name: "person[name]", class: "form-control"}
          %label Age:
          %input{type: "text", name: "person[age]", class: "form-control"}
          %input{:type => "submit", :value => "Save", class: "btn btn-default"}

%div.row
  %div.col-lg-6
    %table.table.table-striped
      %thead
        %tr
          %th Name
          %th Age
          %th.short Options

      %tbody
      - @people.each do |person|
        %tr
          %td= person.name
          %td= person.age
          %td
            %a{href: "/people/#{person.id}", class: "btn btn-xs btn-info"} Show
